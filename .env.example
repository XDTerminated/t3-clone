GEMINI_API_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
DATABASE_URL=
DIRECT_DATABASE_URL=

# For better Neon connection handling, add these parameters to your DATABASE_URL:
# ?pgbouncer=true&connection_limit=1&pool_timeout=20&connect_timeout=60

# Example DATABASE_URL format for Neon:
# postgresql://username:password@host:5432/database?pgbouncer=true&connection_limit=1&pool_timeout=20&connect_timeout=60

# DIRECT_DATABASE_URL should be the same as DATABASE_URL but without pgbouncer parameter (for migrations)
# postgresql://username:password@host:5432/database